# ADR-0001 — review loop log

`review-contract` writes one row per loop. The log lives here rather than in
the ADR, following the same reasoning as `standards-review-log.md`: that
document has never carried one, no project standard requires one, and a review
table is not what a decision record's readers open it for.

Genre is pinned `adr` on every run, so the cap is 3 loops and Q4 is not asked.

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Outcome |
|------|------|-------|----|----|----|---------|
| 1 | 2026-09-03 | 3, cold — genre pinned `adr`; trigger SNAT-0057, the whole document new | 3 | 2 | 1 | **Six verified, six fixed; none dismissed. First gate on this document.** **All three lanes independently found the same defect**, the strongest signal in the run: the ADR stated that a digest mismatch "is never skipped", while `scripts/build-linux.sh` ships a documented path that skips the check entirely — setting `SNATCH_APPIMAGETOOL` substitutes a caller's own packer and reaches `chmod +x` with nothing compared, printing `(unverified, caller's choice)`. A maintainer applying the rule literally would delete that branch as a breach, or add a digest check that defeats its purpose; an auditor would record the packer as always verified. **Two lanes found the Decision sentence false as written.** "Every artifact the build downloads is pinned … and verified against a recorded SHA-256" does not hold for PyPI: CI installs `ruff`, `pytest` and `pyinstaller` at exact versions and `requirements.txt` by version, with no `--require-hashes`. Verified by opening the install steps. `docs/building.md` was already correct here — it reserves its "(SHA-256 pinned)" annotation for the binary rows — so the ADR was the outlier, and the header line calling SNAT-0035 "the one open exception" compounded it. The section is now § Known exceptions and names all three. **Two lanes found the Consequences line contradicting the mpv bullet**: "a two-step edit — version, then digest" against "`MPV_WIN_TAG`, `MPV_WIN_ASSET` and the asset's digest move together", so a bump routine built from the consequence names an asset that does not exist. **Three single-lane findings, all material.** The ADR called a dated release tag "the only immutable identifier that repository offers" while its own exception section says a tag is a pointer its owner can move — two answers to what counts as an acceptable pin, and a builder taking up SNAT-0035 could close it with an exact tag under one and only a SHA under the other. It claimed "nothing in this repository watches for a newer yt-dlp" when `scripts/update-ytdlp-pin.sh` re-resolves the pin and has a `--check` mode that exits non-zero when behind — so an implementer would write a second resolver rather than call the one that ships; confirmed nothing schedules it, which is the true half of the claim. And the policy was silent on downloads the running app makes: the in-app yt-dlp self-update verifies against the release's own `SHA2-256SUMS` manifest, a deliberate second model, and conforming it to the recorded-digest rule is not possible. **Two collisions from this loop's own fixes, caught at 4a step 4**: the title still said "everything the build fetches" after the Decision was narrowed, and the signatures alternative argued against partial coverage in a document that had just admitted three gaps — reworded to argue for gaps being *enumerable*, which is the real distinction. **Ledgered `out_of_scope`:** `digest_for()`'s header comment in `scripts/fetch-binaries.sh` still says the mpv asset name "is resolved from the API at fetch time", which SNAT-0064 removed. All three lanes raised it. Code-side, so not carried into this document. **Open questions resolved clean, not counted:** whether `zizmor` runs in any gate (it does not; the ADR only reports what it says when run, which is accurate), and whether the pip installs are hash-pinned (they are not, which is what made finding 2 real rather than a wording gap). |
| 2 | 2026-09-03 | 3, cold — identical brief, packet rebuilt from disk and given the windows loop 1's open questions showed were missing | 3 | 1 | 0 | **Four verified, four fixed; none dismissed. Three of the four landed on text loop 1 itself wrote**, which is what a cold second loop is for. **All three lanes found the same two.** Loop 1 added a Consequences bullet pointing at `scripts/update-ytdlp-pin.sh` — and that script rewrites `YTDLP_VERSION` and the version in `docs/building.md` and never touches `digest_for()`, which holds a digest per yt-dlp asset. So the endorsed release step produces exactly the state rule 2 calls a build that cannot succeed, and the document gave two incompatible bump procedures. Verified by reading the script to its end. Loop 1's PyPI exception also under-enumerated its own gap: each build job runs `pip install --upgrade pip`, pinned by nothing, so anyone closing that exception over a lock file would leave three unpinned self-upgrades — and the Alternatives section rests on the gaps being enumerable. **Two lanes found loop 1's rule-1 qualification false as written**: it named `SNATCH_APPIMAGETOOL` as *the* path reaching a tool without a digest, and mpv's cache reuse is a second — `stamp_matches` compares a tag stamp where every other asset uses `cached_ok`, a content compare whose own comment says a stamp must not be trusted for this. Recorded as a fourth known exception and filed as SNAT-0065; the code fix is not this gate's to make. **One was pre-existing, from the original draft:** "A release build downloads its dependencies fresh rather than reusing a cache" is false of everything this decision scopes itself to — `fetch()` returns early on `cached_ok` — and the cache SNAT-0047 dealt with is GitHub's pip cache, which the Decision puts out of scope. An implementer could have stripped the `cached_ok` fast path to conform. One lane filed it, one declined on different grounds, one raised it as an open question; the code settled it. **Open questions resolved clean, not counted:** whether the pin script refreshes digests further down (it does not), whether CI sets `SNATCH_APPIMAGETOOL` (it does not, so that claim holds), and whether `zizmor`'s present tense misleads given it runs in no gate — two lanes judged it immaterial and I agree, the sentence being true of what the tool reports when run. |
| 3 | 2026-09-03 | 3, cold — identical brief, packet rebuilt and re-measured | 5 | 0 | 0 | **Five verified, five fixed. Cap reached (3 for an ADR); the run files its tail and exits. A CALM cap — 2 of the 5 landed on text this run wrote, the other 3 on the original draft**, so the document held more defects than the cap held loops rather than the run oscillating. **All three lanes found the same one**: the Consequences bullet named `bin/` as "the cache the decision governs" when the Decision scopes itself to two scripts and `build-linux.sh` caches at `.cache/runtime-*` and the repo-root `appimagetool` — so anyone cleaning, ignoring or auditing those caches would cover one of two. **The sharpest finding is a live code defect, not a wording one.** `digest_for` keys on the asset FILENAME and the yt-dlp destination is version-independent, so after a version-only bump a populated `bin/` still holds the old binary matching the still-unchanged digest: `cached_ok` succeeds, nothing downloads, and the build goes green having bundled the previous nightly. CI is unaffected — a clean checkout has nothing cached and hard-stops — so the failure is local-only, which is worse than it sounds because that is where a developer tests the bump. Filed as SNAT-0067; the ADR now states both halves. **Two lanes, different angles, one passage:** rule 2 said to take the digest "the mismatch message prints", which fails for exactly the pin the document singles out — when the asset FILENAME changes (mpv, or a new platform) `digest_for` has no entry, so the missing-digest stop fires and prints no hash to copy. The other lane added the trust point: `docs/building.md` prescribes upstream's published `.digest`, which records what upstream vouches for rather than what this machine received. Rule 2 now says both. **One lane overturned a dismissal two earlier loops had made**: `zizmor` was described in the present tense while it runs in no job and no local gate. Loops 1 and 2 called it immaterial; loop 3 named the line — someone closing SNAT-0035 reads the gap as already gated, SHA-pins the actions and skips adding `zizmor`, leaving absent the regression guard the ADR itself says must land with the pins. **One attribution error:** rule 1 quoted "add it to `digest_for()`…" as the mismatch message; it is the no-recorded-digest branch, and the mismatch branch prints expected and computed hashes instead. Rule 1 now describes both stops. **Packet defect, mine, disputed correctly by a lane:** my SNAT-0047 summary read "conditional on a tag build" where the code is conditional on NOT being one. The ADR was right; the packet was wrong. **Filed rather than fixed here:** SNAT-0066 (PyPI installs pinned by version only, including an unpinned `pip` self-upgrade) and SNAT-0067 above, so exception 2 and the new consequence cite real items instead of saying nothing tracks them. **Open questions resolved clean, not counted:** neither `build-windows.sh` nor `build-macos.sh` fetches anything, so the scope sentence is complete; CI never sets `SNATCH_APPIMAGETOOL`; the pin script does rewrite `docs/building.md`; and the quoted `cached_ok` comment is verbatim. **Whole-run measure:** the document is new in this change, so every finding fell inside the gated span — this run was a gate throughout, with no audit half. |
