# STANDARDS.md — review loop log

`review-contract` writes one row per loop. The log lives here rather than in
STANDARDS.md: that document has never carried one, no project standard requires
one, and a review table is not what its readers open it for.

Genre is pinned `standard` on every run, so the cap is 3 loops and Q4 is not
asked.

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Outcome |
|------|------|-------|----|----|----|---------|
| 1 | 2026-09-02 | 3, cold — genre pinned `standard`; trigger SNAT-0056 | 4 | 4 | 1 | **Nine verified, nine fixed; none dismissed.** First gate ever run on this document. **Three defects were found independently by all three lanes**, the strongest signal in the run. §7.1 and §7.2 stated the config and history location as `{project_root}/…` while §14 says the location is decided by `app_data_dir()` and is not the project root in a packaged build — an implementer following §7 writes user data inside the macOS `.app` bundle or into the AppImage temp dir that is deleted on exit. §14 claimed "The directory itself is created `0o700`" unconditionally, and `app_data_dir()` calls `_ensure_dir` on only two of its four branches; measured on this machine, the dev-mode directory is 0o755, so a conformer treating the containing directory as private is wrong on Windows and from source. And §12.2's `_get_base_cmd` block iterated `self._cached_runtimes` with the `self._ensure_runtime_cache()` line missing, so anyone copying the documented pattern gets `TypeError: 'NoneType' object is not iterable` — or drops the loop and reintroduces the silent no-JS-runtime format loss the block exists to prevent. **The Q3 was the one this gate was triggered for.** §5.3 was rewritten on 2026-09-01 to prescribe `atomic_private_write`, and it states the whole obligation as "write it through the helper" — but the 0o600 guarantee for a file that already exists comes from `utils.PRIVATE_DATA_FILES` driving a startup repair pass, which the section never mentions. A conformer adding a fourth sensitive file is fully compliant as written and is silently outside that pass forever. **Three more were single-lane and all material.** §5.2 prescribed how to use `pkexec` safely when the project no longer escalates at all — the only `pkexec` left in the tree is a docstring recording its removal, so the standard would have had someone reintroduce it. §8.2 listed two JS runtimes where the code probes four, so refreshing the list from that table drops `quickjs` and `bun` and the bundled `qjs` stops being selected. And §3.1 said the active theme "is referenced throughout the app via a module-level variable" while every consumer calls `get_theme()` and nothing outside `theme.py` touches `active_theme` — with §10.4's own import example naming `DarkTheme`, which no module imports; following it binds the theme at import and the widget stops following theme switches. Also fixed: STANDARDS said Python 3.8+ in two places where README promises 3.10 or newer, and an orphaned `icon.png` table row sat below §14's prose asserting a "Project root" location that same prose denies. **Two open questions resolved clean and are not counted:** `HAS_PIL` is defined in `downloader.py`, so §2.1 and §8.4 are correct as written; and §9's `Escape` row is a shortcut reference, not a build contract. **Ledgered `out_of_scope`:** CLAUDE.md calls `HAS_PIL` "detected at import" alongside two flags that are — a defect in a neighbouring document, not carried into this one. |
